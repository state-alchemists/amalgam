tasks:

  createMyFrontendMigration:
    icon: 🔧
    extend: zrbRunShellScript
    location: ../../myApp
    dependencies:
      - prepareMyFrontend
    configRefs:
      - createMyFrontendMigration
      - myFrontend
    envRef: myFrontend
    envs:
      MIGRATION_RUN_ALL:
        default: 1
      IS_GENERATING_MIGRATION:
        default: 1
