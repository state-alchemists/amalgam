tasks:

  startMyFrontend:
    icon: ⚛️
    extend: zrbStartApp
    location: ../../myApp
    inputs:
      - runMyFrontendInLocal
    dependencies:
      - prepareMyFrontend
      - migrateMyFrontend
      - startMyRabbitmq
    configRefs:
      - startMyFrontend
      - myFrontend
    envRef: myFrontend

  prepareMyFrontend:
    icon: 🔧
    extend: zrbRunShellScript
    location: ../../myApp
    configRefs:
      - prepareMyFrontend
      - myFrontend
    envRef: myFrontend
    dependencies:
      - prepareMyApp

  testMyFrontend:
    icon: ✅
    extend: zrbRunShellScript
    location: ../../myApp
    dependencies:
      - prepareMyFrontend
    configRefs:
      - testMyFrontend
      - myFrontend
    envRef: myFrontend

  migrateMyFrontend:
    icon: 🦆
    extend: zrbRunShellScript
    location: ../../myApp
    dependencies:
      - prepareMyFrontend
      - migrateMyApp
    configRefs:
      - migrateMyFrontend
      - myFrontend
    envRef: myFrontend

