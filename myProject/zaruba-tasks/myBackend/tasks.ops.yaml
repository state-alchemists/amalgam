tasks:

  createMyBackendMigration:
    icon: 🔧
    extend: zrbRunShellScript
    location: ../../myApp
    dependencies:
      - prepareMyBackend
    configRefs:
      - createMyBackendMigration
      - myBackend
    envRef: myBackend
    envs:
      MIGRATION_RUN_ALL:
        default: 1
      IS_GENERATING_MIGRATION:
        default: 1
