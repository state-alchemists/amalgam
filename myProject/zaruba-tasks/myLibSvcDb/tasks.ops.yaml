tasks:

  queryMyLibSvcDb:
    icon: 📜
    extend: zrbRunInDockerContainer
    location: ../../myLibSvcDb
    dependencies:
      - startMyLibSvcDbContainer
    syncEnv: false
    configRef: myLibSvcDbContainer
    envRef: myLibSvcDb
    inputs:
      - sql
    configs:
      templateLocation: '{{ .GetProjectPath "zaruba-tasks/myLibSvcDb/queryTemplate" }}'
