tasks:

  createMyAppMigration:
    icon: 🔧
    extend: zrbRunShellScript
    location: ../../myApp
    dependencies:
      - prepareMyApp
    configRefs:
      - createMyAppMigration
      - myApp
    envRef: myApp
    envs:
      MIGRATION_RUN_ALL:
        default: 1
      IS_GENERATING_MIGRATION:
        default: 1
