tasks:

  createMyAuthSvcMigration:
    icon: 🔧
    extend: zrbRunShellScript
    location: ../../myApp
    dependencies:
      - prepareMyAuthSvc
    configRefs:
      - createMyAuthSvcMigration
      - myAuthSvc
    envRef: myAuthSvc
    envs:
      MIGRATION_RUN_ALL:
        default: 1
      IS_GENERATING_MIGRATION:
        default: 1
