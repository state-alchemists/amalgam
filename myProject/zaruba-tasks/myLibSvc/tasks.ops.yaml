tasks:

  createMyLibSvcMigration:
    icon: 🔧
    extend: zrbRunShellScript
    location: ../../myApp
    dependencies:
      - prepareMyLibSvc
    configRefs:
      - createMyLibSvcMigration
      - myLibSvc
    envRef: myLibSvc
    envs:
      MIGRATION_RUN_ALL:
        default: 1
